---
"php-prose": patch
---

External link `class` attributes are now appended to any existing classes on the link instead of overwriting them. All other `externalLinkAttributes` still overwrite as before.
